name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    types: [ opened, synchronize ]
    branches: [ main ]

jobs:
  quality:
    uses: prinsfrank/CI-PHP/.github/workflows/quality.yml@4a5f904a
    with:
      PHP_VERSION: '8.3'
    secrets:
      GH_TOKEN: ${{ github.token }}
